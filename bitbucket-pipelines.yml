pipelines:
  default:
    - step:
        caches:
          - gradle
        script:
          - ./gradlew clean build copyRuntimeLibs batchZip --parallel
          - cd wikiservice
          - ./sls deploy
          - cd ../boardservice
          - ./sls deploy
